# 界鉴项目协作约束

本文件是界鉴项目所有人工与自动化开发工作的入口约束，适用于本目录及其全部子目录。

## 一、权威文档

开始设计、创建、修改或审查代码前，必须阅读与任务相关的规范：

1. docs/02_开发规范/项目设计规范.md：总体架构、目录、状态机、输入输出、安全和测试规范。
2. docs/05_开发记录/开发路线图.md：初步开发顺序、阶段目标和验收条件。
3. docs/03_技术决策/：已经接受的架构决策记录。

代码、测试和文档不得静默偏离上述规范。

## 二、规范修订

实际开发发现规范存在错误、冲突、不可实施或明显增加风险时，不得为了迁就旧文档而继续错误实现，也不得只修改代码：

1. 先说明发现的问题、证据和影响范围。
2. 涉及架构、入口、状态机、持久化、安全边界或公共接口时，新增或更新架构决策记录。
3. 在同一变更中更新 docs/02_开发规范/项目设计规范.md 或 docs/05_开发记录/开发路线图.md。
4. 为规范变化补充迁移说明和对应测试。
5. 更新规范末尾的修订记录。

当前用户明确指令优先于项目规范；接受新的用户决策后，应同步更新本地规范。

## 三、架构硬约束

- 首版采用模块化单体，不主动拆分微服务。
- GUI、CLI和CI必须复用同一应用服务与领域引擎。
- API进程不得直接执行浏览器操作、攻击变异或目标请求。
- 高风险执行必须进入Worker和隔离Runner。
- 领域模型与纯算法不得依赖FastAPI、SQLAlchemy、Playwright或具体LLM SDK。
- 生命周期状态与安全结论必须分开保存。
- LLM只能提出候选规则或解释结果，不能决定最终漏洞结论。
- 运行产物、缓存、凭证和证据不得写入源码目录或提交到Git。
- 未经明确授权，不得扫描公网目标或执行生产环境破坏性测试。

## 四、工程风格

- 保持结构内聚，避免为了形式拆分大量很小的函数或模块。
- 私有辅助函数默认应至少有三个生产代码调用点；测试调用不计入生产复用证据。
- 框架入口、领域不变量、安全边界、独立失败或清理职责，以及能够阻止主流程膨胀的独立检查，可以低于三个生产调用点，但必须能够说明其边界价值。
- 零调用且没有框架发现机制、当前需求或测试约束的符号应删除或延后实现，不得仅以未来可能使用为由保留。
- 新增抽象前必须证明存在两个以上真实实现或明确的隔离需求。
- 三百至五百非注释代码行只是模块职责审查提醒，不是文件最低行数，不得为凑行数合并不相关职责。超过约五百非注释代码行时应审查是否混合了多个演进方向；小文件仅在缺乏独立业务、框架或安全边界时合并。
- `__init__`、程序入口、配置、Schema 和测试文件不要求承担三百行以上业务逻辑。
- 用户可见阶段名称保持简单：接入、录制、建约、测试、验证、报告。
- 内部状态可以更细，但不得直接暴露给普通用户。
- 公共数据结构必须带schema_version。
- 公共接口和文件格式发生不兼容变化时必须提供迁移说明。
- 日志、异常、事件和报告必须经过统一脱敏。

## 五、Python验证

- Python检查必须同时设置 PYTHONDONTWRITEBYTECODE=1 并使用 python -B。
- 语法检查优先使用ast.parse，不使用py_compile或compileall。
- 不得在源码目录生成或修改__pycache__与.pyc文件。
- 需要导入检查时，使用项目解释器的-B参数，并确认检查前后没有新增缓存文件。

## 六、提交前检查

每次实现至少确认：

- 是否遵守当前权威规范和已接受决策。
- 是否引入新的秘密、未脱敏日志或越界网络访问。
- 状态转换、失败恢复和取消路径是否有测试。
- 副作用动作是否具备预算、观察和清理策略。
- 文档、Schema、API客户端和实现是否保持一致。
- 实际开发若改变了设计，规范是否已经同步更新。

## 七、会话、工作区与权限

- 本项目唯一有效工作区根目录是 `D:\常用\BJUT\全国大学生信息安全竞赛\2027\界鉴`。新建 Codex 项目或执行线程前，必须确认保存项目的 `path` 与该目录完全一致，不得使用上一级 `2027` 目录代替。
- 已有“工程骨架-执行线程”继续承接工程骨架相关实施。新执行线程统一使用“任务名-执行线程”，不添加“界鉴-”前缀。
- 项目根目录必须保持继承开启，由 `LHQ\LHQ` 所有，并为 `LHQ\LHQ` 保留可向子目录和文件继承的 `FullControl`。新增文件必须继承该权限，不得创建受保护的独立 ACL。
- 普通代码修改继续使用工作区沙箱。只有 ACL 调整、用户级依赖缓存、系统临时目录或其他明确超出工作区的必要操作，才申请范围明确的提升权限。
- 新执行线程第一次写入前，先读取根目录 ACL；如需验证写入，只能在 `var/` 创建唯一命名的临时探针，读回后立即删除并确认目录恢复原状。
- 会话不得自行执行 `takeown`、递归 `icacls /reset`、关闭继承或删除未知 SID。发现权限异常时先报告当前身份、目标路径、所有者、继承状态和失败命令，由主线程决定修复范围。
- 完成任务所必需的命令若因权限、沙箱、用户级缓存、系统临时目录或用户上下文差异失败，经一次证据核对确认不是代码或测试断言失败后，应停止无增量重试并直接交给用户执行。交接必须给出工作目录、可复制的精确命令、用途与安全影响、预期结果以及需要回传的输出；用户返回结果后再从该恢复点继续。
- 无法解析的 SID 可能是已结束沙箱会话的权限遗留。仍有相关会话运行时不清理；确需清理时，必须先关闭使用相关父目录的全部会话，再从产生显式条目的父目录进行单独审计。

## 八、Sol / Luna 协作治理

### 1. 角色与执行线程

- Sol 主线程负责目标澄清、仓库调查、架构与阶段边界、任务交接、异常决策和最终验收；当方案、范围和验收条件已经明确时，原则上不继续承担机械实现。
- Luna 执行线程负责在交接范围内完成实现、定向测试和本次变更直接引入的局部修复；关键假设失效或重新出现架构选择时，应返回 Sol 决策。
- 一个 Luna 默认负责一个完整阶段或若干高度相关的阶段。进入新主题、上下文明显膨胀或执行质量下降时，由 Sol 基于当前仓库重新生成精炼交接，再启用新的执行线程。
- 同一工作树同一时间只保留一个主要写入线程。不得使用临时子智能体冒充指定 Luna；需要并行时必须使用独立工作树，并明确互不重叠的 Git 和文件边界。

### 2. 任务交接与范围

Sol 向 Luna 的交接固定包含：背景、目标、已确定决策、可以修改、不可以修改、验收标准、最低验证和升级条件。交接描述结果与边界，不默认展开成逐行操作清单；已经确定的方案仅在关键假设失效时重新讨论。

- 不得因顺手优化扩大任务，不修改无关模块、依赖、公共接口、稳定格式、用户自己的改动或其他阶段代码。
- 新旧执行线程之间只传递下一阶段需要的完成状态、冻结决策、可依赖接口、范围、验收和风险；长期事实应沉淀到代码、测试、ADR、规范和 Git，而不是依赖完整聊天历史。

### 3. 升级条件

出现以下任一情况时，Luna 停止重复试错并返回 Sol：

- 实际代码与关键假设冲突，或完成验收必须修改范围外的重要模块。
- 需要改变架构、安全边界、公共 API、公共 Schema、持久格式或核心状态机语义。
- 同一问题经过若干合理方案仍失败，开始出现重复尝试或无法解释的环境差异。
- 需要人工权限、安装软件、外部账号，或需要改写 Git 历史及执行其他高风险操作。

升级报告应说明问题、已尝试内容、失败原因、最可能根因、1～3 个下一步方案，以及分别需要 Sol 或用户决定的事项。

### 4. 验证与失败处理

- 开发子阶段只运行定向测试和最小功能验证；相关子阶段形成里程碑后再做必要集成与回归。只有安全边界、数据迁移、核心状态机、公共协议、工件一致性、崩溃恢复、阶段合并或最终里程碑等高风险节点才扩大验证。
- 不在同一代码状态上重复无增量价值的全量测试或静态检查。Sol 验收先检查范围、验收条件、架构风险和 Git 清洁度，仅在高风险、报告存疑、环境差异或动态验收必要时重跑关键验证。
- 失败方案必须记录所用方法、失败原因、根因和错误假设。可重复的失败模式应转化为规则、测试、ADR、校验项或交接检查项，不能只以“下次注意”结束。

### 5. 汇报与推理效率

- 交接后若没有新的 Sol 决策事项，不持续轮询执行线程，不重复读取相同代码、重做既定架构分析或进行无新信息的推理；等待完成、阻塞、关键假设失效、范围扩大或重大设计问题事件。
- Sol 仅在架构、公共接口、多方案权衡、复杂调查和失败重规划时提高 reasoning effort。Luna 默认使用 high；仅对边界明确的复杂实现或局部疑难问题使用 xhigh，架构不确定时返回 Sol。
- Luna 完成报告固定使用“变更摘要、修改文件、验证结果、偏离原方案、待确认问题”五部分；没有偏离或待确认问题时明确写“无”。
