# 界鉴 Codex 启动协议

> 状态：CURRENT。适用于本仓库及全部子目录。本文只负责开工门禁、最小知识路由、全局硬边界和停止条件；详细规则从 `docs/llms.txt` 进入唯一知识库。

## 一、先确认用户真的要求行动

只有当前用户消息明确要求分析、判断、设计、修改、测试、提交、委派或其他可识别交付结果时，才进入相应工作。用户只提供背景、文件、交接状态、计划或参考资料而没有要求当前行动时，只记录边界并等待，不自行检查仓库、运行测试、修改文件、设计方案或启动执行线程。

“你怎么看”“是否合理”“为什么失败”等问题已经是明确的分析任务，不要求必须出现祈使句。只读分析不授权写文件、提交、推送、外部调用或其他副作用。

## 二、新对话、压缩或中断后先恢复事实

长期任务在新对话、上下文压缩、明显中断或用户要求继续时，继续设计、写入或委派前必须重新确认：

1. 最新用户指令和当前明确交付物；
2. 当前 `HEAD`、分支和 working tree；
3. 最近已接受恢复点、阶段账本或完成报告；
4. `docs/llms.txt` 的当前任务路由；
5. 阶段任务的完整阶段提示词及当前执行卡。

旧聊天、摘要、提交名称和旧路径只能帮助定位，不能单独证明任务已完成、授权仍有效或工作树没有变化。阶段提示词始终是完整范围真源，不得被局部计划、卡片或摘要缩减。

阶段账本或交接文档要求只读取顶部、当前阶段或指定章节时，必须逐行按语义标题边界停止；不得用固定行数、固定字节数或全文读取后截取来猜边界。输出正文前必须确认下一章节的停止标题已经命中；未命中时停止并报告，不能继续输出后续阶段。

## 三、按任务加载最少且强制的知识

进入任何非平凡任务先读 `docs/llms.txt`。命中下表后，所列文件必须在设计、代码调查、写入或委派之前读取，不是可选建议。

| 任务类型 | 强制前置知识 |
| --- | --- |
| 版本任务、主线程、恢复、验收 | 完整任务提示词、当前账本/执行卡、[任务执行与恢复](docs/04_工程约束/任务执行与恢复.md)；涉及委派时再读[执行线程协作](docs/04_工程约束/执行线程协作.md) |
| 普通生产代码 | `docs/llms.txt` 指向的当前任务指南、模块参考，以及必要的[工程设计](docs/04_工程约束/工程设计.md) |
| 测试、验证、提交 | [验证与测试](docs/04_工程约束/验证与测试.md)；再按被测模块读取对应知识 |
| Protocol、Schema、公共格式 | [公共数据与 Schema 版本](docs/03_参考手册/协议/公共数据与Schema版本.md)、相关协议和验证约束 |
| 数据库、Storage、Migration | [数据与持久化](docs/01_系统地图/数据与持久化.md)、验证约束 |
| AGENTS、Docs、README、注释治理 | [知识与文档治理](docs/04_工程约束/知识与文档治理.md)、任务执行约束；改源码注释时再读 `.agents/skills/jiejian-comments/SKILL.md` |
| Git、沙箱、ACL、权限 | [工作区与权限](docs/04_工程约束/工作区与权限.md)、任务执行约束 |
| 权限判断、Verification、PASS/BLOCK/INCONCLUSIVE | [权限验证与结果](docs/01_系统地图/权限验证与结果.md)、[权限契约与执行计划](docs/03_参考手册/协议/权限契约与执行计划.md) |

普通任务第一轮主动上下文默认限制为：

```text
docs/llms.txt
+ 1 篇当前任务说明
+ 1 篇当前模块说明
+ 确实需要的 1 篇工程约束或协议参考
```

只有发现明确跨模块、协议、安全、数据库或运行边界后才继续下钻。不得为了“保险”开局读取全部系统地图、ADR、Schema、测试和生产源码；始终以 `docs/llms.txt` 的当前路由目标为准。

## 四、所有任务都不能违反的边界

- 唯一有效工作区是当前 Git 仓库根；同一工作树同一时间只保留一个主要写入者。
- 界鉴当前生产 Target 只有 Web。GUI、CLI 和 CI 复用 ApplicationCore；API 进程不直接执行浏览器、高风险目标请求或攻击变异，这些操作必须进入 Worker 和隔离 Runner。
- Verification 只根据已形成事实给出 `PASS`、`BLOCK`、`INCONCLUSIVE`。生命周期、错误原因和安全结论分开；LLM 不得决定 Verdict、Finding、Gate 或自动激活权限规则。
- 秘密只使用受控引用和最小运行时注入。密码、Cookie、Token、API Key 不得进入普通数据库字段、公共协议、Evidence、Finding、Report、日志、异常正文、源码或 Git。
- 运行数据、缓存、证据和生成物只进入 `var/` 等运行边界，不得写回源码目录。
- 只有独立持久化、跨进程或对外交换且有独立 reader 的根文档携带 `schema_version`；嵌套 DTO 不重复版本。公共格式、数据库断代和安全语义变化由主线程裁决。
- 未经明确授权不得扫描公网目标、执行生产破坏性测试、调用真实付费接口、修改凭据或改写 Git 历史。

## 五、实施与验证

- 先核对当前代码、测试、Git 和运行环境实态，再按已接受设计实施；发现文档与代码实质冲突时明确指出并决定应修正哪一方，不偷偷选择。
- Python 检查同时设置 `PYTHONDONTWRITEBYTECODE=1` 并使用 `python -B`，语法优先 `ast.parse`。正式 pytest 只能经 `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev.ps1 test ...`；前端测试只能经 `dev.ps1 frontend-test`。
- 验证按“模块范围 × L1～L4 程度 × 是否需要 L5”选择最小充分证据；同一代码状态不重复 L4。失败后先区分产品、测试/fixture 与环境，再只重跑失败项和直接邻域。
- 修改项目自有源码或测试后，最终验证前使用 `.agents/skills/jiejian-comments/SKILL.md` 校准注释；项目自有 Python 文件至少保留一行职责性 `#` 文件头。
- 副作用结果不明确时先回读当前事实，不得重复执行提交、推送、删除、数据库写入、凭据写入或真实外部调用来试探第一次是否成功。

## 六、停止、升级与执行线程接受

- 架构、安全、公共语义、文档治理、AGENTS、用户交互、前端视觉和最终产品验收由主线程裁决。执行线程只承接边界已冻结、可独立验收的机械实现。
- 出现范围扩大、架构或安全选择、公共接口/Schema/持久化变化、第二次非偶发返工、外部账号/权限阻塞或 Git 历史改写时，停止重复试错并按协作规范升级。
- `执行线程停止 ≠ 任务完成 ≠ 主线程已经接受`。正式接受 handoff 必须同时取得可读完成报告、Git/working tree 实态、修改范围、验证证据和主线程审计结论。
- 主线程在 `WAITING` 不并发修改同一工作树、不提前开始下一批、不进行连续主动轮询。主线程创建的一次性线程在报告回收和验收后必须归档，工具支持时继续删除。
- 阶段验收只覆盖完整提示词和已接受任务明确列出的能力。范围外非阻塞缺口进入遗留事项；只有缺口直接否定阶段成立时才重新划分边界。
