# 界鉴项目协作约束

本文件是界鉴项目所有人工与自动化开发工作的入口约束，适用于本目录及其全部子目录。

## 一、执行顺序与事实真源

处理任务时按以下顺序判断：

1. 明确当前用户指令和任务边界。
2. 阅读与任务相关的权威文档。
3. 核对当前代码、测试、Git 和运行环境实态。
4. 按架构、安全和工程约束实施。
5. 运行与风险相称的验证，并检查提交范围。
6. 遇到规范冲突、权限阻塞或架构选择时，按本文件的升级规则处理。

### 1. 权威文档

开始设计、创建、修改或审查代码前，必须阅读与任务相关的规范：

1. `docs/02_开发规范/项目设计规范.md`：总体架构、目录、状态机、输入输出、安全和测试规范。
2. `docs/05_开发记录/开发路线图.md`：初步开发顺序、阶段目标和验收条件。
3. `docs/03_技术决策/`：已经接受的架构决策记录。

代码、测试和文档不得静默偏离上述规范。当前用户明确指令优先于项目规范；接受新的用户决策后，应同步更新本地规范。

### 2. 规范修订

实际开发发现规范存在错误、冲突、不可实施或明显增加风险时，不得为了迁就旧文档而继续错误实现，也不得只修改代码。必须：

1. 先说明发现的问题、证据和影响范围。
2. 涉及架构、入口、状态机、持久化、安全边界或公共接口时，新增或更新架构决策记录。
3. 在同一变更中更新 `docs/02_开发规范/项目设计规范.md` 或 `docs/05_开发记录/开发路线图.md`。
4. 为规范变化补充迁移说明和对应测试。
5. 更新规范末尾的修订记录。

## 二、架构与安全硬约束

### 1. 系统边界

- 首版采用模块化单体，不主动拆分微服务。
- GUI、CLI 和 CI 必须复用同一应用服务与领域引擎。
- API 进程不得直接执行浏览器操作、攻击变异或目标请求。
- 高风险执行必须进入 Worker 和隔离 Runner。
- 领域模型与纯算法不得依赖 FastAPI、SQLAlchemy、Playwright 或具体 LLM SDK。
- 生命周期状态与安全结论必须分开保存。
- LLM 只能提出候选规则或解释结果，不能决定最终漏洞结论。

### 2. 数据与目标安全

- 运行产物、缓存、凭证和证据不得写入源码目录或提交到 Git。
- 日志、异常、事件和报告必须经过统一脱敏。
- 未经明确授权，不得扫描公网目标或执行生产环境破坏性测试。
- 公共数据结构必须带 `schema_version`。
- 公共接口和文件格式发生不兼容变化时必须提供迁移说明。

## 三、工程风格

### 1. 模块与抽象

- 保持结构内聚，避免为了形式拆分大量很小的函数或模块。
- 私有辅助函数默认应至少有三个生产代码调用点；测试调用不计入生产复用证据。
- 框架入口、领域不变量、安全边界、独立失败或清理职责，以及能够阻止主流程膨胀的独立检查，可以低于三个生产调用点，但必须能够说明其边界价值。
- 零调用且没有框架发现机制、当前需求或测试约束的符号应删除或延后实现，不得仅以未来可能使用为由保留。
- 新增抽象前必须证明存在两个以上真实实现或明确的隔离需求。
- 三百至五百非注释代码行只是模块职责审查提醒，不是文件最低行数，不得为凑行数合并不相关职责。超过约五百非注释代码行时应审查是否混合了多个演进方向；小文件仅在缺乏独立业务、框架或安全边界时合并。
- `__init__`、程序入口、配置、Schema 和测试文件不要求承担三百行以上业务逻辑。

### 2. 用户可见表达

- 用户可见阶段名称保持简单：接入、录制、建约、测试、验证、报告。
- 内部状态可以更细，但不得直接暴露给普通用户。

## 四、验证与提交

### 1. Python 验证

- Python 检查必须同时设置 `PYTHONDONTWRITEBYTECODE=1` 并使用 `python -B`。
- 语法检查优先使用 `ast.parse`，不使用 `py_compile` 或 `compileall`。
- 不得在源码目录生成或修改 `__pycache__` 与 `.pyc` 文件。
- 需要导入检查时，使用项目解释器的 `-B` 参数，并确认检查前后没有新增缓存文件。
- 普通 pytest 验证默认使用 `-p no:cacheprovider`，并将唯一命名的 `--basetemp` 放在工作区 `var/` 下；验证结束后精确清理本次临时目录。
- 只有需要 `--lf`、`--ff` 或 `--stepwise` 排错时才临时启用 `.pytest_cache`；子阶段结束后只清理已确认的仓库内缓存，不递归清理上级目录或仓库外路径。

### 2. 提交前检查

每次实现至少确认：

- 是否遵守当前权威规范和已接受决策。
- 是否引入新的秘密、未脱敏日志或越界网络访问。
- 状态转换、失败恢复和取消路径是否有测试。
- 副作用动作是否具备预算、观察和清理策略。
- 文档、Schema、API 客户端和实现是否保持一致。
- 实际开发若改变了设计，规范是否已经同步更新。

## 五、工作区、沙箱与权限

### 1. 固定工作区

- 本项目唯一有效工作区根目录是 `D:\常用\BJUT\全国大学生信息安全竞赛\2027\界鉴`。新建 Codex 项目或执行线程前，必须确认保存项目的 `path` 与该目录完全一致，不得使用上一级 `2027` 目录代替。
- 已有“工程骨架-执行线程”继续承接工程骨架相关实施。新执行线程统一使用“任务名-执行线程”，不添加“界鉴-”前缀。
- 项目根目录必须保持继承开启，由 `LHQ\LHQ` 所有，并为 `LHQ\LHQ` 保留可向子目录和文件继承的 `FullControl`。新增文件必须继承该权限，不得创建受保护的独立 ACL。

### 2. 沙箱使用与首次探针

- 普通代码修改继续使用工作区沙箱。只有 ACL 调整、用户级依赖缓存、系统临时目录或其他明确超出工作区的必要操作，才申请范围明确的提升权限。
- 新执行线程第一次写入前，只进行一次低成本环境探针：确认准确工作区、沙箱身份、根目录 ACL、Git 状态和本阶段实际使用的运行时。
- 如需验证写入，只能在 `var/` 创建唯一命名的临时探针，读回后立即删除并确认目录恢复原状。
- 涉及 Node 子进程、浏览器或 pytest 临时目录时，分别增加一个最小定向探针；探针通过后不重复执行。

### 3. 权限失败交接

完成任务所必需的命令若因权限、沙箱、用户级缓存、系统临时目录或用户上下文差异失败，先进行一次证据核对，确认它不是代码或测试断言失败。确认后停止无增量重试，直接交给用户执行。交接必须包含：

- 工作目录。
- 可复制的精确命令。
- 用途与安全影响。
- 预期结果。
- 需要用户回传的输出。

用户返回结果后，从该恢复点继续。

### 4. ACL 与 SID 诊断

- 会话不得自行执行 `takeown`、递归 `icacls /reset`、关闭继承或删除未知 SID。发现权限异常时，先报告当前身份、目标路径、所有者、继承状态和失败命令，由主线程决定修复范围。
- 无法解析的 SID 不等于陈旧账户。Windows elevated 沙箱会对当前写入根同时授予稳定的 `CodexSandboxUsers` 权限和可能无法解析为普通账户的 capability SID，并对 `.git` 等受保护路径设置对应 DENY。
- capability SID 是任务或写入根的授权主体，不是必须能在“本地用户和组”中解析的普通账户。新建任务仍可能为新的写入根增加此类 SID，不能以已有 `CodexSandboxUsers` 或改用新任务为由假设它不会出现。
- 判断 capability SID 前，必须核对最新 sandbox 日志、当前写入根和该 SID 是否被本次 setup 重新授予；仍被当前会话使用的 capability SID 不得清理。
- 只有在关闭使用相关父目录的全部会话后，SID 不再被最新 setup 日志引用或重新创建，且已定位其显式 ACL 来源时，才能作为历史残留处理。
- 清理前必须备份相关路径 SDDL，只移除已确认的显式 ACE，不递归、不修改继承项；工具无法匹配孤立 SID 时不得继续猜测命令变体。
- `apply deny-read ACLs` 应优先核对 `%USERPROFILE%\.codex\.sandbox\deny_read_acl_state.json` 是否为无效 JSON 或 NUL 内容。
- `CreateProcessAsUserW failed: 5` 应优先核对实际 shell 可执行文件及父目录的读取执行权限。
- 上述两类错误均不得通过重置项目 ACL 处理。

### 5. Git 保护

- `.git` 的沙箱写入拒绝默认视为预期保护。
- `git add`、`git commit`、`git merge` 等操作按批准规则申请沙箱外执行。
- 申请因环境权限失败并完成一次证据核对后，按“权限失败交接”的格式交给用户，不通过放宽 `.git` ACL 绕过。

## 六、Sol / Luna 协作治理

### 1. 角色与线程定义

- Sol 主线程负责目标澄清、仓库调查、架构与阶段边界、任务交接、异常决策和最终验收；当方案、范围和验收条件已经明确时，原则上不继续承担机械实现。
- Luna 执行线程负责在交接范围内完成实现、定向测试和本次变更直接引入的局部修复；关键假设失效或重新出现架构选择时，应返回 Sol 决策。
- 本文件中的 Luna 或执行线程，只指 Codex 应用中用户可见、可单独进入和归档的任务线程。
- 内部 sub-agent 即使被命名为“正式 Luna”，也不得创建、冒充或代替指定执行线程。找不到、无法唤醒或无法交接给既有 Luna 时，Sol 必须停止调度并请用户提供或创建对应任务，不得自行降级为内部 sub-agent。

### 2. 模型、粒度与生命周期

- Sol 创建用户可见的执行线程时，必须显式指定 `gpt-5.6-luna` 与 `high` reasoning effort；不得省略模型而继承 Sol 默认配置。只有用户当次明确指定其他模型或 reasoning effort 时才覆盖该默认值。
- 一个 Luna 默认连续负责同一完整阶段或若干高度相关的阶段；子阶段/批次边界不等于执行线程边界。每批完成恢复点验收后，默认继续复用当前 Luna。
- Sol 每次只交接一个边界完整、能够独立验收的子阶段；存储、公共 API、GUI 和动态集成等具有不同验证闭环的内容应分批交接。
- 子阶段完成并形成恢复点后，必须先验收该恢复点；每个子阶段必须以可恢复状态结束：优先形成清晰提交；暂不提交时必须明确当前提交、未提交范围、验证结果和下一子阶段可依赖接口。
- 进入不同能力域、前后端切换或目录变化本身不构成新建线程理由。
- 新建 Luna 只能由以下实际证据之一触发：
  - 现有上下文已实际造成遗漏、误解或返工。
  - 收到一次明确的固定运行时纠正后，仍重复使用错误环境。
  - 执行质量或指令遵循出现可观察下降。
  - 预计剩余单个自然子阶段已无法在当前上下文中可靠完成。
  - 下一个任务是可独立验收的不同结果，且确有并行或隔离价值。
- 换线程前，Sol 必须记录具体触发证据，并从当前提交、完成报告和冻结约束生成精炼交接；没有证据时继续复用当前 Luna。
- 执行线程若被系统中断，新线程应从仓库、权威文档和最近完成报告恢复，不依赖重放完整聊天历史。
- 同一工作树同一时间只保留一个主要写入线程。需要并行时必须使用独立工作树，并明确互不重叠的 Git 和文件边界。

- Sol 在阶段首次探针确认实际运行时后，后续交接必须写明本阶段所需解释器、Node、npm 等工具的绝对路径和版本。
- Luna 必须使用交接指定的绝对路径，不得自行改用 PATH 默认环境、安装或升级依赖，亦不得 fallback；路径缺失或版本不符时立即升级，不反复尝试。
- 同一线程且环境未变化时复用首次探针结果，不机械重复完整环境探针。
- 完成报告必须写明实际使用的运行时绝对路径、版本和完整验证命令。

### 3. 任务交接与范围

Sol 向 Luna 的交接固定包含：背景、目标、已确定决策、可以修改、不可以修改、验收标准、最低验证和升级条件。

- 初次交接固定包含上述完整字段；同一 Luna 的后续交接以增量为主，只补当前恢复点、本批目标、增量约束、验收与升级条件，不重复全部稳定背景，但验收与升级条件不能省略。
- 交接描述结果与边界，不默认展开成逐行操作清单；已经确定的方案仅在关键假设失效时重新讨论。
- 不得因顺手优化扩大任务，不修改无关模块、依赖、公共接口、稳定格式、用户自己的改动或其他阶段代码。
- 换线程时只传递下一阶段需要的完成状态、冻结决策、可依赖接口、范围、验收和风险；同一 Luna 的后续批次按上述增量交接规则执行。
- 长期事实应沉淀到代码、测试、ADR、规范和 Git，而不是依赖完整聊天历史。

### 4. 升级条件

出现以下任一情况时，Luna 停止重复试错并返回 Sol：

- 实际代码与关键假设冲突，或完成验收必须修改范围外的重要模块。
- 需要改变架构、安全边界、公共 API、公共 Schema、持久格式或核心状态机语义。
- 同一问题经过若干合理方案仍失败，开始出现重复尝试或无法解释的环境差异。
- 需要人工权限、安装软件、外部账号，或需要改写 Git 历史及执行其他高风险操作。

升级报告必须说明：问题、已尝试内容、失败原因、最可能根因、1～3 个下一步方案，以及分别需要 Sol 或用户决定的事项。

### 5. 验证与失败处理

- 开发子阶段只运行定向测试和最小功能验证；相关子阶段形成里程碑后再做必要集成与回归。
- 只有安全边界、数据迁移、核心状态机、公共协议、工件一致性、崩溃恢复、阶段合并或最终里程碑等高风险节点才扩大验证。
- 不在同一代码状态上重复无增量价值的全量测试或静态检查。
- Sol 验收先检查范围、验收条件、架构风险和 Git 清洁度，仅在高风险、报告存疑、环境差异或动态验收必要时重跑关键验证。
- 失败方案必须记录所用方法、失败原因、根因和错误假设。可重复的失败模式应转化为规则、测试、ADR、校验项或交接检查项，不能只以“下次注意”结束。

### 6. 汇报、等待与推理效率

- 交接后若没有新的 Sol 决策事项，不持续轮询执行线程，不重复读取相同代码、重做既定架构分析或进行无新信息的推理；等待完成、阻塞、关键假设失效、范围扩大或重大设计问题事件。
- Sol 仅在架构、公共接口、多方案权衡、复杂调查和失败重规划时提高 reasoning effort。
- Luna 默认使用 `high`；仅对边界明确的复杂实现或局部疑难问题使用 `xhigh`，架构不确定时返回 Sol。
- Luna 完成报告固定使用“变更摘要、修改文件、验证结果、偏离原方案、待确认问题”五部分；没有偏离或待确认问题时明确写“无”。
