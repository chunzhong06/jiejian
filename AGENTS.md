# 界鉴项目协作约束

本文件是界鉴项目所有人工与自动化开发工作的入口约束，适用于本目录及其全部子目录。

## 一、权威文档

开始设计、创建、修改或审查代码前，必须阅读与任务相关的规范：

1. docs/PROJECT_SPEC.md：总体架构、目录、状态机、输入输出、安全和测试规范。
2. docs/INITIAL_DEVELOPMENT_PLAN.md：初步开发顺序、阶段目标和验收条件。
3. docs/decisions/：已经接受的架构决策记录。

代码、测试和文档不得静默偏离上述规范。

## 二、规范修订

实际开发发现规范存在错误、冲突、不可实施或明显增加风险时，不得为了迁就旧文档而继续错误实现，也不得只修改代码：

1. 先说明发现的问题、证据和影响范围。
2. 涉及架构、入口、状态机、持久化、安全边界或公共接口时，新增或更新架构决策记录。
3. 在同一变更中更新 docs/PROJECT_SPEC.md 或 docs/INITIAL_DEVELOPMENT_PLAN.md。
4. 为规范变化补充迁移说明和对应测试。
5. 更新规范末尾的修订记录。

当前用户明确指令优先于项目规范；接受新的用户决策后，应同步更新本地规范。

## 三、架构硬约束

- 首版采用模块化单体，不主动拆分微服务。
- GUI、CLI和CI必须复用同一应用服务与领域引擎。
- API进程不得直接执行浏览器操作、攻击变异或目标请求。
- 高风险执行必须进入Worker和隔离Runner。
- 领域模型与纯算法不得依赖FastAPI、SQLAlchemy、Playwright或具体LLM SDK。
- 生命周期状态与安全结论必须分开保存。
- LLM只能提出候选规则或解释结果，不能决定最终漏洞结论。
- 运行产物、缓存、凭证和证据不得写入源码目录或提交到Git。
- 未经明确授权，不得扫描公网目标或执行生产环境破坏性测试。

## 四、工程风格

- 保持结构内聚，避免为了形式拆分大量很小的函数或模块。
- 私有辅助函数默认应至少有三个生产代码调用点；测试调用不计入生产复用证据。
- 框架入口、领域不变量、安全边界、独立失败或清理职责，以及能够阻止主流程膨胀的独立检查，可以低于三个生产调用点，但必须能够说明其边界价值。
- 零调用且没有框架发现机制、当前需求或测试约束的符号应删除或延后实现，不得仅以未来可能使用为由保留。
- 新增抽象前必须证明存在两个以上真实实现或明确的隔离需求。
- 三百至五百非注释代码行只是模块职责审查提醒，不是文件最低行数，不得为凑行数合并不相关职责。超过约五百非注释代码行时应审查是否混合了多个演进方向；小文件仅在缺乏独立业务、框架或安全边界时合并。
- `__init__`、程序入口、配置、Schema 和测试文件不要求承担三百行以上业务逻辑。
- 用户可见阶段名称保持简单：接入、录制、建约、测试、验证、报告。
- 内部状态可以更细，但不得直接暴露给普通用户。
- 公共数据结构必须带schema_version。
- 公共接口和文件格式发生不兼容变化时必须提供迁移说明。
- 日志、异常、事件和报告必须经过统一脱敏。

## 五、Python验证

- Python检查必须同时设置 PYTHONDONTWRITEBYTECODE=1 并使用 python -B。
- 语法检查优先使用ast.parse，不使用py_compile或compileall。
- 不得在源码目录生成或修改__pycache__与.pyc文件。
- 需要导入检查时，使用项目解释器的-B参数，并确认检查前后没有新增缓存文件。

## 六、提交前检查

每次实现至少确认：

- 是否遵守当前权威规范和已接受决策。
- 是否引入新的秘密、未脱敏日志或越界网络访问。
- 状态转换、失败恢复和取消路径是否有测试。
- 副作用动作是否具备预算、观察和清理策略。
- 文档、Schema、API客户端和实现是否保持一致。
- 实际开发若改变了设计，规范是否已经同步更新。

## 七、会话、工作区与权限

- 本项目唯一有效工作区根目录是 `D:\常用\BJUT\全国大学生信息安全竞赛\2027\界鉴`。新建 Codex 项目或执行线程前，必须确认保存项目的 `path` 与该目录完全一致，不得使用上一级 `2027` 目录代替。
- 已有“工程骨架-执行线程”继续承接工程骨架相关实施。新执行线程统一使用“任务名-执行线程”，不添加“界鉴-”前缀。
- 项目根目录必须保持继承开启，由 `LHQ\LHQ` 所有，并为 `LHQ\LHQ` 保留可向子目录和文件继承的 `FullControl`。新增文件必须继承该权限，不得创建受保护的独立 ACL。
- 普通代码修改继续使用工作区沙箱。只有 ACL 调整、用户级依赖缓存、系统临时目录或其他明确超出工作区的必要操作，才申请范围明确的提升权限。
- 新执行线程第一次写入前，先读取根目录 ACL；如需验证写入，只能在 `var/` 创建唯一命名的临时探针，读回后立即删除并确认目录恢复原状。
- 会话不得自行执行 `takeown`、递归 `icacls /reset`、关闭继承或删除未知 SID。发现权限异常时先报告当前身份、目标路径、所有者、继承状态和失败命令，由主线程决定修复范围。
- 无法解析的 SID 可能是已结束沙箱会话的权限遗留。仍有相关会话运行时不清理；确需清理时，必须先关闭使用相关父目录的全部会话，再从产生显式条目的父目录进行单独审计。
