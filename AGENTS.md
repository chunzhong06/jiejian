# 界鉴项目协作约束

> 状态：CURRENT。适用于本目录及全部子目录。本文件只保留所有任务都必须立即知道的入口和硬约束；专题规则按任务从 `docs/README.md` 加载。

## 一、执行顺序与事实真源

处理任务时按以下顺序判断：

1. 明确当前用户指令、完整任务提示词和边界。
2. 阅读 `docs/README.md` 路由的 1～3 篇任务相关 Architecture，以及必要 ADR、Protocol、Schema 或 Migration。
3. 核对当前代码、测试、Git 和运行环境实态。
4. 按架构、安全和工程约束实施。
5. 按 [验证与提交规范](docs/01_技术规范/验证与提交规范.md) 运行与风险相称的最小验证并检查提交范围。
6. 遇到规范冲突、权限阻塞、架构选择或范围扩大时，按专题规范升级，不自行猜测。

当前用户指令优先于项目规范。一个阶段可以继续使用一份完整的大阶段提示词；它始终是范围、禁令和最终验收真源，不因中间规划或分批执行而被拆成多个互不完整的局部真源。设计线程规划、主线程执行和执行线程子阶段交接都必须保持对该完整提示词的可追溯映射，详见 [协作与阶段执行规范](docs/01_技术规范/协作与阶段执行规范.md)。

用户只提供背景而未明确当前动作或交付物时，只确认边界，不启动仓库检查、测试、修改或设计；完整用户意图门禁见[协作与阶段执行规范](docs/01_技术规范/协作与阶段执行规范.md)。

发现规范错误、冲突或不可实施时，不得只迁就旧文档修改代码：先说明证据与影响；涉及架构、入口、状态机、持久化、安全或公共接口时同步 Architecture，长期决策同步 ADR，机器边界同步 Protocol/Schema/Migration，路线或验收边界变化时才更新 Roadmap。

## 二、架构与安全硬约束

- 首版采用模块化单体；GUI、CLI 和 CI 复用同一应用服务与领域引擎。
- API 进程不得直接执行浏览器操作、攻击变异或目标请求；高风险执行必须进入 Worker 和隔离 Runner。
- 领域模型与纯算法不得依赖 FastAPI、SQLAlchemy、Playwright 或具体 LLM SDK。
- 生命周期状态与安全结论分开保存；LLM 只能提出候选或解释结果，不能决定最终漏洞结论。
- 首次使用的目录选择与项目识别只经 `ApplicationCore` onboarding 能力；选择不扫描，识别只在受限 allowlist、深度、文件数和字节预算内只读执行，不运行目标命令、不联网、不读取源码正文或秘密。
- 运行产物、缓存、凭证和证据只进入 `var/` 等运行边界，不得写入源码目录或提交到 Git。
- 日志、异常、事件和报告统一脱敏；未经明确授权不得扫描公网目标或执行生产环境破坏性测试。
- 只有可独立持久化、跨进程或对外交换且存在独立读取边界的根文档携带 `schema_version`；嵌套 DTO 不重复版本。每个根文档只接受一个当前格式，不兼容变化必须提供迁移或明确重建说明。

模块、依赖、抽象、注释、Windows 脚本编码和用户可见术语的完整规则见 [工程设计规范](docs/01_技术规范/工程设计规范.md)。

## 三、任务开始与验证摘要

- 唯一有效工作区是当前 Git 仓库根；不得以上级目录或同名副本代替。
- 新执行线程第一次写入前只做一次低成本环境探针；环境未变化时复用结果，不机械重复。
- Python 检查同时设置 `PYTHONDONTWRITEBYTECODE=1` 并使用 `python -B`；语法优先 `ast.parse`。仓库 pytest 统一通过 `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\scripts\dev.ps1 test ...` 运行，由该入口注入受控 Python 身份、禁用 cacheprovider，并在 `var/test/` 创建和精确清理唯一 basetemp；不得把裸 `python -m pytest` 的结果作为正式验证证据。
- 验证范围按“模块范围 × L1～L4 验证程度 × 是否需要 L5”确定；局部改动从最小直接证据开始，只有出现明确升级证据才扩大。同一代码状态不重复 L4。
- 测试失败先区分产品、测试/fixture 和环境工具；修复后先只重跑失败项与直接邻域。
- 修改项目自有源码或测试后，最终验证前使用 `.agents/skills/jiejian-comments/SKILL.md` 校准注释；所有项目自有 Python 文件（包括测试、fixture 和 migration version）至少保留一行职责性 `#` 文件头。

完整测试合同、L1～L5 定义和提交前检查见 [验证与提交规范](docs/01_技术规范/验证与提交规范.md)；沙箱、ACL、权限交接和 Git 保护见 [工作区与权限规范](docs/01_技术规范/工作区与权限规范.md)。

## 四、协作与范围控制摘要

- 设计线程负责完整阶段提示词下的中间调查、方案比较、详细规划与设计；主线程依据冻结方案实施、集成并最终验收。
- 设计方案必须显式映射完整提示词，不得取代、缩减或扩大原始范围；关键假设失效时返回设计线程或用户重新决策。
- 主线程可把低判断、边界已冻结的机械实现交给执行线程；架构、安全、公共语义、文档治理、AGENTS、用户交互和最终产品验收保留在主线程。
- 同一工作树同一时间只保留一个主要写入者。每个自然子阶段先明确“必须完成、明确不做、停止条件”，并以提交或清晰恢复点结束。
- 第二次非偶发返工、范围外公共能力、架构或安全选择、权限/外部账号阻塞、Git 历史改写等情况必须停止重复试错并升级。
- 阶段验收只覆盖提示词和已接受任务明确列出的能力；非阻塞缺口列入遗留事项，不在收口时扩项。

角色分工、线程生命周期、交接字段、运行时固定、技术实验、汇报和范围熔断的完整规则见 [协作与阶段执行规范](docs/01_技术规范/协作与阶段执行规范.md)。
