# ADR-0030：契约与执行快照绑定

- 状态：已接受
- 日期：2026-08-19
- 适用范围：ACTIVE Contract、ExecutionProfile、执行计划和冻结请求

## 背景

用户配置、当前治理版本和真正执行的输入具有不同生命周期。执行期间重新读取可变 Profile、Contract 或 Flow，会使历史结果无法解释，也可能让治理变化悄悄改变目标请求。

## 决策

ExecutionProfile 是内部执行说明书，当前只允许 WEB target，并显式保存 Contract 引用、身份、action/observer bindings、预算、secret refs 和指纹。Profile 只由 SecuritySetupCompiler 从当前确认事实生成，内容寻址且不可原地修改；用户控制面不提供上传、选择或改写入口。内部 ExecutionWorkflow 登记时检查项目治理绑定、ACTIVE 状态、计划、来源 fingerprint 和 authority fingerprint，漂移默认拒绝。

提交时将 ACTIVE Contract、Coverage/PermissionMutationPlan、bindings、目标范围、预算和指纹冻结为 `ExecutionProjectSnapshot`，再写入 `PersistedExecutionRequest`。Worker 和 Runner 只消费冻结请求，不重新读取可变 Profile、Contract 或 Flow。

## 理由与取舍

快照绑定保证结果可复现、可审计并阻止治理漂移影响运行。代价是配置更新必须重新登记或显式接受变化，旧请求不会自动跟随当前配置。

## 影响

执行请求同时保留 `contract_id`、`contract_version`、plan/binding fingerprint 和完整执行快照；旧 Run 的安全语义不因新版本激活而改变。相同普通输入复用同一内容 hash；输入变化产生新 Profile 文件并使旧生成配置停止提交，但不删除历史文件。

## 迁移与兼容

缺少绑定、版本不匹配、fingerprint 不一致或未知结构时严格失败。旧开发 Profile、Run 和请求格式不提供兼容读取。

## 相关真源

- [安全意图与验证架构](../01_系统地图/权限验证与结果.md)
- [执行与观察架构](../01_系统地图/执行与观察.md)
- [Web 执行配置与冻结快照](../03_参考手册/协议/Web执行配置与冻结快照.md)
- `product/protocols/web/profile.py`
- `product/protocols/execution_request.py`
