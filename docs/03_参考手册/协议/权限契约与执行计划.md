# 权限契约与执行计划

> 状态：CURRENT。本文解释权限数据族之间的安全边界，不复制 JSON Schema 字段表。

## 目的与消费者

本协议族连接 Contract Governance、Coverage 编译、WebExecutionProfile、Verification 和 GUI/CLI 展示。消费者需要区分用户意图、候选建议、可执行契约、覆盖用例和观察要求。

## 先理解：用户需求、执行计划和结论不是同一份数据

用户确认“Bob 不得导出 Alice 的项目资料”时，`PermissionIntent` 保存的是主体权限组、资源所有者权限组、关系、action 与 DENY 预期，不保存 Bob/Alice 的具体账号、Cookie、HTTP 或 Observer。确定性编译器再根据当前 Flow、TestIdentity 代表、资源、观察和恢复事实生成可执行 Contract/Profile；Coverage 和 Differential 负责产生 ALLOW control 与 DENY variant。

执行后形成的 Evidence 与 Verdict 不回写 PermissionIntent。换一个测试账号可能让旧 Profile 失效，却不能静默改变用户已经确认的权限需求；缺代表形成 coverage gap，也不能删除 DENY。

## 协议边界

- `Requirement` 是待验证意图。
- `ContractCandidate` 是确定性分析建议，始终不可信；当前入口不接受模型生成 Candidate。
- `PermissionContract`/`ContractVersion` 是唯一可执行安全意图，包含 Security Effect Manifest；Action 只通过 `effect_ids` 引用效果，不包含 transport 细节。
- `NormalizedPermissionPlan` 是规范化计划。
- `PermissionMutationPlan` 是带 fingerprint、seed、预算、cases、coverage、gaps 和 required observations 的 Contract-only 覆盖计划。
- `DifferentialExperimentPlan` 在 Coverage 之后为风险 case 冻结 ALLOW 控制、DENY 变体、允许变化维度和必须相同的不变量。
- `Coverage` 说明规则、subject、action、resource、relation 和覆盖范围；不包含 HTTP transport 细节。

## 生命周期与数据流

```text
Requirement → Candidate → 人工治理
  → PermissionContract/ContractVersion ACTIVE
  → NormalizedPermissionPlan
  → PermissionMutationPlan/Coverage
  → DifferentialExperimentPlan
  → WebExecutionSnapshot
```

确定性 canonical、ID、排序、指纹和 drift 结果必须稳定。历史 LLM 来源记录不构成当前可创建的 Candidate 类型。

## 失败与安全语义

空规则、重复 ID、跨项目引用、冲突候选、观察器不足、规则漂移、非法状态转换和 canonical/hash 不一致均阻止进入执行。Candidate 不能决定 Verdict、Finding、Gate 或自动激活 Contract。

## 版本规则与 Schema 真源

`PermissionContract`、`NormalizedPermissionPlan`、`PermissionMutationPlan` 与 `DifferentialExperimentPlan` 当前根 `schema_version` 都以字符串 1 作为 Web V1 发布基线；`PermissionContract.version` 继续表示独立的业务治理版本。完整字段、required、枚举和严格解析以：

- `product/backend/core/`
- `product/protocols/schemas/contracts/`
- `product/protocols` 中对应模型

为准。本文只说明边界，不复制完整 Schema。

## 查询入口

| 要查什么 | 当前真源 |
| --- | --- |
| 普通 PermissionIntent | `product/backend/workflows/permission_intents.py` |
| Contract、Coverage、Differential | `product/backend/core/verification/permissions/` |
| 确定性安全准备编译 | `product/backend/workflows/security_setup/` |
| Contract Schema 与协议模型 | `product/protocols/schemas/contracts/`、`product/protocols/` |
| 直接测试 | `tests/backend/core/verification/permissions/`、`tests/backend/workflows/security_setup/` |

## 相关真源

- [安全意图与验证架构](../../01_系统地图/权限验证与结果.md)
- [安全意图与契约治理边界](../../05_设计依据/ADR-0029-安全意图与契约治理边界.md)
- [契约与执行快照绑定](../../05_设计依据/ADR-0030-契约与执行快照绑定.md)
