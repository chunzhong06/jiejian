# 系统总体架构

> 状态：CURRENT。当前适用范围：本机模块化单体、Web 产品入口、权限安全检查和结果交付。

## 职责

说明模块化单体的组合根、入口、控制面、持久任务、隔离执行和当前 Web 目标边界，提供其他系统地图的总览。

## 非职责

- 不定义 Runner、Observer、Report 或公共 Schema 的完整字段。
- 不复制 ADR 历史或路线图。
- 不把 CLI_APPLICATION、MCP_AGENT 或未来适配器描述为当前能力。
- 不替代代码、测试、migration 和协议 Schema。

## 真源

当前结构真源是 `product/backend`、`product/protocols`、`product/frontend`、`scripts`、相关测试和现行 migration。

## 目的

真实表达“入口如何进入共享应用、应用如何进入受控执行、结果如何返回”，避免入口、领域和执行职责混淆。

## 当前产品边界

当前主链到持续维护 Human Approval 为止：`ApplicationUnderstanding` 只产生带证据的 Candidate；`BusinessBoundaryService` 从完整 current 边界建立 desired-state 草稿，并在服务端规划引用、追加 revision、实现重绑与停用；本机 GUI 只编辑和审阅不可变 Proposal。只有固定 `LOCAL_GUI` 审批事务能原子提交业务 revision、Permission、Binding、Decision 与新的 `policy_epoch`。

`WorkspaceService` 从 Project、ApplicationUnderstanding、当前 Business Boundary、Permission、实时 Binding inspection 与 PreparationView 形成只读 `WorkspaceView` 和唯一 `PrimaryTask`；前端不重算优先级或 currentness。既有 Verification、Worker、Runner、Evidence、Finding 和 Report 子系统仍保留，但 没有把完整新检查主链重新接入当前 GUI。

## 组件与边界

- GUI、CLI 和 API 共享 `product/backend/composition/application.py` 中完整的 `ApplicationCore`；Worker 使用 `composition/worker.py` 中独立、完整的 `WorkerContainer`，不通过继承或部分初始化共享组合根。
- `backend/core` 保存领域规则和安全结论；`workflows` 编排应用动作；`infra` 提供 Storage、队列、事务和发布；`api` 只做 transport；`cli` 只做命令适配。
- Worker 消费持久 Job，维护租约、fencing、取消、重试、发布和恢复。
- 受控 Runner 执行主动目标流量，并把受限结果交回 Worker；这里的隔离只指解释器、环境、进程树、取消和恢复边界，不代表未来 CLI Target 所需的完整 OS 沙箱。
- `TargetRuntimeRegistry` 按当前 Target Kind 创建 runtime；生产只注册 Web Target Runtime，通用 Case Orchestrator 与 Verification 不依赖 HTTP 协议。
- `RuntimePaths` 是 `var/data`、`runtime`、`cache`、`logs`、`temp` 和 `test` 的唯一物理路径真源。
- Run publication 与 Verdict 先独立完成，`ResultFinalizer` 再幂等物化 Finding/Occurrence 和基础报告；Gate 是可选后续派生。
- `product/protocols` 是跨进程机器协议真源；前端不直接调用目标。

## 关键数据流

```text
用户/CI
  → GUI、CLI 或 loopback API
  → ApplicationCore
  → Contract/Profile 校验与冻结执行请求
  → 持久 Job → Worker → 隔离 Runner
  → TargetRuntimeRegistry → Web Target Runtime
  → Evidence → 唯一 Verification → Run publication
  → ResultFinalizer → Finding/Occurrence → 基础报告
  → 可选 Baseline/Gate/Gate 报告
  → API、CLI、GUI 和 CI 读取已发布结果
```

安全意图到结果的完整链路见[安全意图与验证架构](./权限验证与结果.md)。

## 关键不变量

- API 和 GUI 不直接发高风险目标请求。
- Worker 不重新读取可变 Profile、Flow 或 Contract；执行使用已冻结快照。
- 领域纯逻辑不依赖 FastAPI、SQLAlchemy、Playwright 或具体 LLM SDK。
- 生命周期状态、Verification Verdict 和 GateResult 分离。
- 已发布 Evidence 不可变，结果读取只消费已校验事实。
- Run、Recording、身份准备和其他用户可见任务进入终态后，所有页面与顶栏投影必须从权威事实最终收敛：不得长期同时保留新成功结果与旧失败提示，也不得在后端已无活动任务时继续显示运行中。短暂刷新过程可以存在，但不能形成第二套持久状态真源。

## 失败语义

入口输入、Contract/Profile 漂移、目标范围、预算、协议、租约、完整性或发布校验失败时，系统拒绝进入下一边界并返回脱敏、可识别错误。未知 TargetType、过期 fencing token、未知数据库结构和无法验证的工件均 fail closed。

## 安全约束

执行只允许用户明确授权的 Web 范围；重定向、私网访问、请求数、响应字节、并发、超时、秘密、日志和清理均受边界预算约束。LLM 不能决定安全结论。

## 兼容与版本

当前生产协议只表达 Web Target，不保存 `CLI_APPLICATION`、`MCP_AGENT` 等未实现枚举或空模型。所有界鉴自有独立根文档从字符串 `schema_version="1"` 起步，数据库从不可改写的 `0001_business_boundary_v2` fresh 基线建立；未来 Target 或其他不兼容机器格式通过新的 Schema 版本加入 Registry。旧开发数据库、Profile、Run、Evidence、Artifact、Report 和 wire format 不提供兼容读取；Schema/protocol 版本只表示机器格式。

## 当前可验证产品事实

- 产品面向 Web Target；GUI、CLI 和 API 共享 `ApplicationCore`，Worker 经独立 `WorkerContainer` 装配，高风险目标流量经过 Worker 与隔离 Runner。
- 当前 用户链包含应用接入、显式只读源码分析、业务边界首次建立、desired-state 持续维护、不可变 Proposal 与 Human Approval；权限确认不依赖测试账号、业务流程、Observer 或执行配置。
- `WorkspaceView` 是当前 GUI 的唯一动作级工作区真源；`PrimaryTask` 由服务端固定优先级生成，页面不保存线性进度。
- 已保留的执行子系统包含 Web HTTP 执行、观察事实、Evidence、Verification、Finding、Baseline、Gate 和 Report，但不属于 当前可操作检查主链。

## 当前验收边界

每个能力必须有可复现入口、真实代码和直接测试，并保持 PermissionContract、执行快照、Evidence、Verdict、GateResult 和 Report 分离。新 Target、公共协议、数据库迁移或安全语义必须单独形成决策和验收。

未来目标与实验结论不属于当前实现；当前稳定事实以本节和各模块、任务指南为准。

## 相关真源

- [工程设计规范](../04_工程约束/工程设计.md)
- [安全意图与验证架构](./权限验证与结果.md)
- [执行与观察架构](./执行与观察.md)
- [数据与持久化架构](./数据与持久化.md)
- [产品入口与控制面架构](./产品入口与控制面.md)
- [应用接入与检查主流程](./应用接入与检查主流程.md)
- `product/backend/`、`product/protocols/`、`product/frontend/`、`scripts/`、相关测试
